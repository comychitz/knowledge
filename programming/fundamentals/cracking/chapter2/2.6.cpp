#include <iostream>
#include <string>
#include <vector>

/**
 * Question 
 *
 * Given a circular linked list, implemnent an algorithm which returns the node
 * at the beginning of the loop.
 * DEFINITION
 * Circular linkedl ist: A (corrupt) linked list in which a node's next pointer
 * pointers to an earlier node, so as to make a loop in the linked list.
 * EXAMPLE
 * Input: A->B->C->D->E->C [the same C as before]
 * Output: C
 */


int main(int argc, const char *argv[])
{

  
  return 0;
}
