#include <iostream>
#include <string>
#include <vector>

/**
 * Question 
 * 
 * Given a directed graph, design an algorithm to find out whether there is a
 * route between two nodes.
 *
 * Approach
 * - Do breadth or depth first traversal, see if node was in the visited set.
 */


int main(int argc, const char *argv[])
{

  
  return 0;
}
