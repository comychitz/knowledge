#include <iostream>
#include <string>
#include <vector>

/**
 * Question 
 * 
 * Design and implement a hash table which uses chaining (linked list) to handle
 * collisions.
 *
 * Approach:
 * - vector of pairs. first is the hash, second is the beginning of the linked
 *   list
 * - the result of hashing a key should be the index in the array(!)
 */


int main(int argc, const char *argv[])
{

  
  return 0;
}
